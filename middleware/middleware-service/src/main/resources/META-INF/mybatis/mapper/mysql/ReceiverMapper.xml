<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.deepdragon.entity.weipu.ReceiverMapper">

	<sql id="columnList">
		`id`,`tenantId`,`memberId`,`name`,`email`,`areaId`,`areaPath`,`address`,`zipCode`,`phone`,`mobile`,`isDefault`,`createUser`,`createDate`,`modifyUser`,`modifyDate`
	</sql>

	<select id="get" resultType="net.deepdragon.entity.weipu.Receiver">
		select re.*,ar.fullName as areaName from wp_receiver re
		left join shared_area ar on re.areaPath=ar.path
		where re.id = #{id}
	</select>

	<select id="find" resultType="net.deepdragon.entity.weipu.Receiver">
		select re.*,ar.fullName as areaName from wp_receiver re
		left join shared_area ar on re.areaPath=ar.path
		where re.tenantId = #{tenantId} and re.${property}=#{value}
	</select>

	<select id="findList" resultType="net.deepdragon.entity.weipu.Receiver">
		select re.*,ar.fullName as areaName from wp_receiver re
		left join shared_area ar on re.areaPath=ar.path
		where re.tenantId = #{tenantId} and re.${property}=#{value}
	</select>

	<select id="findListByIds" parameterType="java.lang.String"
		resultType="net.deepdragon.entity.weipu.Receiver">
		select re.*,ar.fullName as areaName from wp_receiver re
		left join shared_area ar on re.areaPath=ar.path
		where re.id in
		<foreach collection="list" item="id" index="index" open="("	close=")" separator=",">#{id}</foreach>
	</select>

	<select id="getAll" resultType="net.deepdragon.entity.weipu.Receiver">
		select re.*,ar.fullName as areaName from wp_receiver re
		left join shared_area ar on re.areaPath=ar.path
		where re.tenantId = #{tenantId}
	</select>

	<select id="getPager" resultType="net.deepdragon.entity.weipu.Receiver">
		select re.*,ar.fullName as areaName from wp_receiver re
		left join shared_area ar on re.areaPath=ar.path
		<if test="(@net.deepdragon.util.OgnlUtils@isEmpty(condition))!=null">${condition}</if>
	</select>

	<select id="getList" resultType="net.deepdragon.entity.weipu.Receiver">
		select re.*,ar.fullName as areaName from wp_receiver re
		left join shared_area ar on re.areaPath=ar.path
		<if test="(@net.deepdragon.util.OgnlUtils@isEmpty(condition))!=null">${condition}</if>
	</select>

	<select id="getTotalCount" resultType="java.lang.Long">
		select count(id) as result
		from wp_receiver where tenantId = #{tenantId}
	</select>

	<select id="isExist" resultType="java.lang.Boolean">
		select count(id) as result from wp_receiver
		where tenantId = #{tenantId} and ${property}=#{value}
	</select>

	<insert id="save" parameterType="net.deepdragon.entity.weipu.Receiver"
		useGeneratedKeys="true" keyProperty="id">
		insert into wp_receiver
		<trim prefix="(" suffix=")" suffixOverrides=",">
			`id`,`tenantId`
			<if test="memberId != null">,`memberId`</if>
			<if test="name != null">,`name`</if>
			<if test="email != null">,`email`</if>
			<if test="areaId != null">,`areaId`</if>
			<if test="areaPath != null">,`areaPath`</if>
			<if test="address != null">,`address`</if>
			<if test="zipCode != null">,`zipCode`</if>
			<if test="phone != null">,`phone`</if>
			<if test="mobile != null">,`mobile`</if>
			<if test="isDefault != null">,`isDefault`</if>
			<if test="createUser != null">,`createUser`</if>
			<if test="createDate != null">,`createDate`</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			#{id},#{tenantId}
			<if test="memberId != null">,#{memberId}</if>
			<if test="name != null">,#{name}</if>
			<if test="email != null">,#{email}</if>
			<if test="areaId != null">,#{areaId}</if>
			<if test="areaPath != null">,#{areaPath}</if>
			<if test="address != null">,#{address}</if>
			<if test="zipCode != null">,#{zipCode}</if>
			<if test="phone != null">,#{phone}</if>
			<if test="mobile != null">,#{mobile}</if>
			<if test="isDefault != null">,#{isDefault}</if>
			<if test="createUser != null">,#{createUser}</if>
			<if test="createDate != null">,#{createDate}</if>
		</trim>
	</insert>

	<update id="update" parameterType="net.deepdragon.entity.weipu.Receiver">
		update wp_receiver
		<set>
			`tenantId` = #{tenantId}
			<if test="memberId != null">,`memberId` = #{memberId}</if>
			<if test="name != null">,`name` = #{name}</if>
			<if test="email != null">,`email` = #{email}</if>
			<if test="areaId != null">,`areaId` = #{areaId}</if>
			<if test="areaPath != null">,`areaPath` = #{areaPath}</if>
			<if test="address != null">,`address` = #{address}</if>
			<if test="zipCode != null">,`zipCode` = #{zipCode}</if>
			<if test="phone != null">,`phone` = #{phone}</if>
			<if test="mobile != null">,`mobile` = #{mobile}</if>
			<if test="isDefault != null">,`isDefault` = #{isDefault}</if>
			<if test="modifyUser != null">,`modifyUser` = #{modifyUser}</if>
			<if test="modifyDate != null">,`modifyDate` = #{modifyDate}</if>
		</set>
		where id = #{id}
	</update>

	<delete id="delete">
		delete from wp_receiver
		where id =	#{id}
	</delete>
	
</mapper>