<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.deepdragon.entity.weipu.ActivityFullcutItemMapper">

	<sql id="columnList">
		`id`,`tenantId`,`fullCutId`,`itemId`,`itemType`,`isDelete`,`createUser`,`createDate`,`modifyUser`,`modifyDate`
	</sql>

	<select id="get" resultType="net.deepdragon.entity.weipu.ActivityFullcutItem">
		select
		<include refid="columnList" />
		from cx_activity_fullcut_item
		where id = #{id}
	</select>

	<select id="find" resultType="net.deepdragon.entity.weipu.ActivityFullcutItem">
		select
		<include refid="columnList" />
		from cx_activity_fullcut_item
		where tenantId = #{tenantId} and ${property}=#{value}
		AND isDelete=1
	</select>

	<select id="findList" resultType="net.deepdragon.entity.weipu.ActivityFullcutItem">
		select
		<include refid="columnList" />
		from cx_activity_fullcut_item
		where tenantId = #{tenantId} and ${property}=#{value}
		AND isDelete=1
	</select>
	
	<select id="findListByIds" parameterType="java.lang.String"
		resultType="net.deepdragon.entity.weipu.ActivityFullcutItem">
		select
		<include refid="columnList" />
		from cx_activity_fullcut_item
		where id in
		<foreach collection="list" item="id" index="index" open="("	close=")" separator=",">#{id}</foreach>
	</select>
	
	<select id="getAll" resultType="net.deepdragon.entity.weipu.ActivityFullcutItem">
		select
		<include refid="columnList" />
		from cx_activity_fullcut_item 
		where tenantId = #{tenantId}
	</select>
	
	<select id="getPager" resultType="net.deepdragon.entity.weipu.ActivityFullcutItem">
		select
		<include refid="columnList" />
		from cx_activity_fullcut_item 
		<if test="(@net.deepdragon.util.OgnlUtils@isEmpty(condition))!=null">${condition}</if>
	</select>
	
	<select id="getList" resultType="net.deepdragon.entity.weipu.ActivityFullcutItem">
		select
		<include refid="columnList" />
		from cx_activity_fullcut_item 
		<if test="(@net.deepdragon.util.OgnlUtils@isEmpty(condition))!=null">${condition}</if>
	</select>

	<select id="getTotalCount" resultType="java.lang.Long">
		select count(id) as result
		from cx_activity_fullcut_item where tenantId = #{tenantId}
	</select>
	
	<select id="isExist" resultType="java.lang.Boolean">
		select count(id) as result from cx_activity_fullcut_item 
		where tenantId = #{tenantId} and ${property}=#{value}
	</select>
	
	<insert id="save" parameterType="net.deepdragon.entity.weipu.ActivityFullcutItem"
		useGeneratedKeys="true" keyProperty="id">
		insert into cx_activity_fullcut_item
		<trim prefix="(" suffix=")" suffixOverrides=",">
			`id`,`tenantId`
			<if test="fullCutId != null">,`fullCutId`</if>
			<if test="itemId != null">,`itemId`</if>
			<if test="itemType != null">,`itemType`</if>
			<if test="isDelete != null">,`isDelete`</if>
			<if test="createUser != null">,`createUser`</if>
			<if test="createDate != null">,`createDate`</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			#{id},#{tenantId}
			<if test="fullCutId != null">,#{fullCutId}</if>
			<if test="itemId != null">,#{itemId}</if>
			<if test="itemType != null">,#{itemType}</if>
			<if test="isDelete != null">,#{isDelete}</if>
			<if test="createUser != null">,#{createUser}</if>
			<if test="createDate != null">,#{createDate}</if>
		</trim>
	</insert>

	<update id="update" parameterType="net.deepdragon.entity.weipu.ActivityFullcutItem">
		update cx_activity_fullcut_item
		<set>
			`tenantId` = #{tenantId}
			<if test="fullCutId != null">,`fullCutId` = #{fullCutId}</if>
			<if test="itemId != null">,`itemId` = #{itemId}</if>
			<if test="itemType != null">,`itemType` = #{itemType}</if>
			<if test="isDelete != null">,`isDelete` = #{isDelete}</if>
			<if test="modifyUser != null">,`modifyUser` = #{modifyUser}</if>
			<if test="modifyDate != null">,`modifyDate` = #{modifyDate}</if>
		</set>
		where id = #{id}
	</update>

	<delete id="delete">
		delete from cx_activity_fullcut_item
		where id =	#{id}
	</delete>
	
</mapper>