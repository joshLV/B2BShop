<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.deepdragon.entity.weipu.MerchantTypeMapper">

	<sql id="columnList">
		`id`,`tenantId`,`name`,`isDefault`,`description`,`orderNo`,`createUser`,`createDate`,`modifyUser`,`modifyDate`
	</sql>

	<select id="get" resultType="net.deepdragon.entity.weipu.MerchantType">
		select
		<include refid="columnList" />
		from wp_merchant_type
		where id = #{id}
	</select>

	<select id="find" resultType="net.deepdragon.entity.weipu.MerchantType">
		select
		<include refid="columnList" />
		from wp_merchant_type
		where tenantId = #{tenantId} and ${property}=#{value}
	</select>

	<select id="findList" resultType="net.deepdragon.entity.weipu.MerchantType">
		select
		<include refid="columnList" />
		from wp_merchant_type
		where tenantId = #{tenantId} and ${property}=#{value}
	</select>
	
	<select id="findListByIds" parameterType="java.lang.String"
		resultType="net.deepdragon.entity.weipu.MerchantType">
		select
		<include refid="columnList" />
		from wp_merchant_type
		where id in
		<foreach collection="list" item="id" index="index" open="("	close=")" separator=",">#{id}</foreach>
	</select>
	
	<select id="getAll" resultType="net.deepdragon.entity.weipu.MerchantType">
		select
		<include refid="columnList" />
		from wp_merchant_type 
		where tenantId = #{tenantId}
	</select>
	
	<select id="getPager" resultType="net.deepdragon.entity.weipu.MerchantType">
		select
		<include refid="columnList" />
		from wp_merchant_type 
		<if test="(@net.deepdragon.util.OgnlUtils@isEmpty(condition))!=null">${condition}</if>
	</select>
	
	<select id="getList" resultType="net.deepdragon.entity.weipu.MerchantType">
		select
		<include refid="columnList" />
		from wp_merchant_type 
		<if test="(@net.deepdragon.util.OgnlUtils@isEmpty(condition))!=null">${condition}</if>
	</select>

	<select id="getTotalCount" resultType="java.lang.Long">
		select count(id) as result
		from wp_merchant_type where tenantId = #{tenantId}
	</select>
	
	<select id="isExist" resultType="java.lang.Boolean">
		select count(id) as result from wp_merchant_type 
		where tenantId = #{tenantId} and ${property}=#{value}
	</select>
	
	<insert id="save" parameterType="net.deepdragon.entity.weipu.MerchantType"
		useGeneratedKeys="true" keyProperty="id">
		insert into wp_merchant_type
		<trim prefix="(" suffix=")" suffixOverrides=",">
			`id`,`tenantId`
			<if test="name != null">,`name`</if>
			<if test="isDefault != null">,`isDefault`</if>
			<if test="description != null">,`description`</if>
			<if test="orderNo != null">,`orderNo`</if>
			<if test="createUser != null">,`createUser`</if>
			<if test="createDate != null">,`createDate`</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			#{id},#{tenantId}
			<if test="name != null">,#{name}</if>
			<if test="isDefault != null">,#{isDefault}</if>
			<if test="description != null">,#{description}</if>
			<if test="orderNo != null">,#{orderNo}</if>
			<if test="createUser != null">,#{createUser}</if>
			<if test="createDate != null">,#{createDate}</if>
		</trim>
	</insert>

	<update id="update" parameterType="net.deepdragon.entity.weipu.MerchantType">
		update wp_merchant_type
		<set>
			`tenantId` = #{tenantId}
			<if test="name != null">,`name` = #{name}</if>
			<if test="isDefault != null">,`isDefault` = #{isDefault}</if>
			<if test="description != null">,`description` = #{description}</if>
			<if test="orderNo != null">,`orderNo` = #{orderNo}</if>
			<if test="modifyUser != null">,`modifyUser` = #{modifyUser}</if>
			<if test="modifyDate != null">,`modifyDate` = #{modifyDate}</if>
		</set>
		where id = #{id}
	</update>

	<delete id="delete">
		delete from wp_merchant_type
		where id =	#{id}
	</delete>
	
	<update id="updateDefault" parameterType="java.util.Map">
		update wp_merchant_type
		<set>
			`isDefault` = 0
		</set>
		where id != #{id}
	</update>
	
</mapper>